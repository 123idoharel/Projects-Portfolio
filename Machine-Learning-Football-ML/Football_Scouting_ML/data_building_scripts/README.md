# scripts used during the base data files creation.
NOTE: some data files were used by these scripts to create these files and saved locally(mapping files, TM values, etc) so can't run those scripts directly without those.
can use those to understand how the TM and Sofascore fetch worked basically
